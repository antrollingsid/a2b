import 'package:flutter/material.dart';

class PaddingTop90 extends StatelessWidget {
  const PaddingTop90({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.only(bottom: 90),
    );
  }
}
